{{#if session.isAuthenticated}}
  Logged in as {{session.currentUser.displayName}}
  <button {{action "signOut"}}>Sign out</button>
  <button {{action "saveIt"}}>save</button>
  {{outlet}}
{{else}}
  <button {{action "signIn" "google"}}>Sign in with Google</button>
{{/if}}